<svg>
  <!-- SDK >= 5 requires this file to exist. This face uses no system widgets,
       so it's intentionally empty (importing widgets_common.defs fails on-device
       with "Invalid path", and we don't need it). -->
</svg>
