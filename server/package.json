{
  "name": "dj-watchface-server",
  "version": "1.0.0",
  "private": true,
  "description": "OAuth + data-proxy server that feeds real Google Health API data into the DJ Watchface web preview",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
